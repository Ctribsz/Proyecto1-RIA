{
  "name": "functions",
  "description": "Cloud Functions para el proyecto ResponsableAI",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions,firestore,hosting",
    "test:integration": "npm run build && firebase emulators:exec --only functions,firestore \"node test-emulator-integration.cjs\"",
    "deploy": "firebase deploy --only functions,firestore,hosting",
    "logs": "firebase functions:log",
    "test": "npm run build && node --test lib/*.test.js lib/**/*.test.js",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^14.2.0",
    "firebase-functions": "^7.3.2",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.15.30",
    "firebase-tools": "^15.26.0",
    "typescript": "^5.8.3"
  },
  "overrides": {
    "uuid": "^11.1.1"
  },
  "private": true
}
